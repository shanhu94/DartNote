import 'package:test/test.dart';
import 'package:learn/f_control_flow.dart' as flow;

void main() {
  test('ifelseControlFlow', () {
    flow.ifelseControlFlow();
  });
  test('forLoop', () {
    flow.forLoop();
  });
  test('whileLoop', () {
    flow.whileLoop();
  });
  test('breakAndContinue', () {
    flow.breakAndContinue();
  });
  test('switchCase', () {
    flow.switchCase();
  });
  test('assertStatement', () {
    flow.assertStatement();
  });
}
